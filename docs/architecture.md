# Arquitetura — Etapa 1

A interface usa módulos ES, roteamento por hash e estado persistido em localStorage. A próxima etapa deverá adicionar os arquivos JSON de regras e o repositório de regras sem acoplar cálculos ao DOM.
